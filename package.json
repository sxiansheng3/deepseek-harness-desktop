{
  "name": "deepseek-harness-desktop",
  "version": "0.1.9",
  "private": true,
  "description": "Desktop shell with independently updatable DeepSeek Harness runtime",
  "author": "tao song",
  "type": "module",
  "main": "src/main.js",
  "scripts": {
    "start": "electron .",
    "icons:win": "node scripts/generate-windows-icon.mjs",
    "test": "node --test test/*.test.js",
    "check": "node --check src/main.js && node --check src/runtime-manager.js && node --check src/bundled-runtime.js && node --check src/desktop-update-manager.js && node --check src/tool-layout.js && node --check src/menu.js && node --check src/preload.cjs && node --check src/windows-runtime-fs-shim.cjs",
    "dist:mac": "electron-builder --config electron-builder.config.cjs --mac dmg zip --arm64",
    "dist:win": "electron-builder --config electron-builder.config.cjs --win nsis --x64"
  },
  "devDependencies": {
    "electron": "43.4.0",
    "electron-builder": "26.15.3",
    "png-to-ico": "3.0.2"
  },
  "dependencies": {
    "electron-updater": "6.8.9"
  }
}
